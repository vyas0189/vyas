[build]
  command = "npm run build"
  publish = "dist"
  functions = ".netlify/functions"
  ignore = "exit 0"

# Sentry DSN is a public value, not a secret
# It's safe to expose in client-side code
[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "SENTRY_DSN"
